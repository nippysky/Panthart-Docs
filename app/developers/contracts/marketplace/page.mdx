export const metadata = { title: 'Marketplace' }

# Marketplace

The core contract that handles listings (fixed price), auctions, and settlements for ERC-721 and ERC-1155. Enforces the **2.5%** fee and routes splits.

<ABIViewer file="marketPlaceCoreABI.json" title="Interface" />

> Events such as `Listed`, `Purchased`, `BidPlaced`, `AuctionSettled`, and `Canceled` are emitted for indexing. See **Developers → Events & Indexing** for guidance.
