import { Callout, Steps } from 'nextra/components'

export const metadata = { title: 'Collection (ERC-721 Drop)' }

# Collection (ERC-721 Drop)

<Callout type="info">
Use a **placeholder** base URI if you plan to reveal later. Keep final metadata immutable once revealed.
</Callout>

<Steps>

### Configure contract
Name, symbol, **max supply**.

### Base URI
Set placeholder or final base URI (IPFS).

### Allowlist / Presale (optional)
Upload addresses, set window, price, per-wallet limits.

### Public mint
Set public price, schedule, and limits.

</Steps>
